# CS2AS 圈子服务

这是上海服务器上的独立实时服务。官网 Worker 只负责账号登录和签发 15 分钟社区令牌；本服务负责 WebSocket 聊天、临时消息和 WebRTC 信令，不保存语音媒体。

当前实现：文本消息、严格连接信息规范化、消息编辑、40 小时消息清理、房间人数限制、WebRTC `signal` 转发。文件上传和每日赞赏机器人在下一步接入时使用同一套令牌与清理边界。

生产部署前必须设置 `COMMUNITY_TOKEN_SECRET`，并通过 HTTPS/WSS 反向代理公开，不能直接把 8787 暴露到公网。
