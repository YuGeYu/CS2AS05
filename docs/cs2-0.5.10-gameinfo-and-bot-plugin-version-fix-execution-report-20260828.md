# CS2 人机增强助手 0.5.10 启动失败修复执行报告

日期：2026-08-28
状态：修复候选，待实机验收

## 已执行

- 运行 `scripts/generate-plugin-manifest.ps1`，从 `package.json` 读取版本并重写内置 ZIP marker。
- marker：`addons/counterstrikesharp/plugins/NadeSystem/CS2AS05.plugin.json`
- marker `version`：`0.5.10`
- marker `schema/product/pluginId`：`2 / cs2-bot-improver / cs2as05-custom-package`
- BotVision provenance：版本 `0.2.2`，source SHA-256 `40B596D34BF336D9E59E663DAC2F94BD7C61D951C56E421EF66B5190B8787290`
- payload SHA-256：`76D9C84D74368DF6727435EAE0C830C7F75056E1E1F6E0AB14C128CD43DC9EF7`
- ZIP：`E:\CS2AS05\src-tauri\resources\CS2BotImprover.zip`
- ZIP SHA-256：`FC8868ABD46056DA52540EB14F3BA0B1B36005928C44178BC479C3BD2A4AA64E`
- ZIP 必需 gameinfo 条目：`gameinfo.gi`、`backup/Online/gameinfo.gi`、`backup/WithBots/gameinfo.gi`、`gameinfo.manifest.json` 均存在；Rust 资源校验会逐条核对 manifest 摘要。

## 代码修复

- `src-tauri/src/services/cs2.rs` 更新内置 ZIP 摘要，避免构建继续接受旧 0.5.9 包。
- BOT 自动安装完成后要求安装目录 marker 与当前 Cargo 版本精确相等；错误包含 `expectedVersion`、`installedVersion`、`zipSha256` 和 marker 路径。
- `src-tauri/src/services/panel.rs` 的 BOT 启动门禁仅对当前精确版本返回 unchanged；其他版本进入安装或明确的更高版本不可信错误。
- gameinfo sidecar 校验现在同时要求 Online/BOT 备份摘要及 `gameinfo.gi.official.bin` 摘要一致；缺失或不一致统一进入 `recoveryRequired`，不会把活动用户文件静默当作官方基线。

## 本地验证

- `cargo test --manifest-path .\\src-tauri\\Cargo.toml cs2`：21 passed，1 ignored（真实 CS2 目录测试按环境变量跳过）。
- `npm test -- --run tests/installer-contract.spec.ts tests/bot-plugin-gate.spec.ts`：9 passed。
- `npm run typecheck`：通过。

## 尚未完成的实机门槛

- 未在全新/备份 CS2 目录执行真实 Steam Online 启动和 BOT 启动；未记录 Steam 参数、插件加载日志或截图。
- 未构建 0.5.10 安装器，因此没有安装器路径和 SHA-256 可报告。
- 尚不能宣称线上发布或“已彻底修复”；下一步应按方案第 5 节完成退出 CS2、安装/更新、Online/BOT 往返及受控 0.5.9 marker 恢复验收。

